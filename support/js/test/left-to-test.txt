 build-list
 build-string
 quicksort
 sort
 andmap
 ormap
 argmin
 argmax
 memf
 apply
 compose
 procedure?




NOTE: inexact->exact isn't quite doing the right thing
We are not supporting:

     apply, 
     exit,
     for-each,
     eof
     eof-object?

at the moment.
